
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .summary { background: #f8f9fa; border-radius: 8px; padding: 20px; margin: 20px 0; }
    .metric { display: flex; justify-content: space-between; margin: 10px 0; }
    .positive { color: #22c55e; }
    .negative { color: #ef4444; }
    .button { background: #2563eb; color: white; padding: 10px 20px; text-decoration: none; border-radius: 4px; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Portfolio Performance Update</h2>
    <div class="summary">
      <div class="metric">
        <span>Total Value:</span>
        <span>$<%= totalValue.toFixed(2) %></span>
      </div>
      <div class="metric">
        <span>Daily Change:</span>
        <span class="<%= dailyChange >= 0 ? 'positive' : 'negative' %>">
          <%= (dailyChange >= 0 ? '+' : '') + dailyChange.toFixed(2) %>%
        </span>
      </div>
      <div class="metric">
        <span>Top Performer:</span>
        <span><%= topPerformer.symbol %> (<%= topPerformer.change >= 0 ? '+' : '' %><%= topPerformer.change.toFixed(2) %>%)</span>
      </div>
    </div>
    <a href="<%= dashboardUrl %>" class="button">View Portfolio</a>
  </div>
</body>
</html>
