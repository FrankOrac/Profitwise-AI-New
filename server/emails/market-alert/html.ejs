
<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .alert { background: #f8f9fa; border-radius: 8px; padding: 20px; margin: 20px 0; }
    .symbol { font-size: 24px; font-weight: bold; color: #1a56db; }
    .details { margin-top: 15px; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Market Alert</h2>
    <div class="alert">
      <div class="symbol"><%= alert.symbol %></div>
      <div class="details">
        <p><strong>Type:</strong> <%= alert.type %></p>
        <p><strong>Message:</strong> <%= alert.message %></p>
        <% if (alert.price) { %>
          <p><strong>Current Price:</strong> $<%= alert.price.toFixed(2) %></p>
        <% } %>
      </div>
    </div>
    <p>
      <small>You received this alert based on your notification preferences. To update your preferences, visit your settings page.</small>
    </p>
  </div>
</body>
</html>
